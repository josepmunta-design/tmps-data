name: Build temporal index

on:
  push:
    branches: [ "main" ]
    paths:
      - "data/Core/escuelas/**"
      - "data/Core/modelos/**"
      - "tools/build-temporal-index.mjs"
      - "package.json"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install deps
        run: npm ci || npm install

     - name: Build temporal index
  run: node tools/build-temporal-index.mjs


      - name: Commit & push temporal index if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add indices/temporal
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "build: update temporal index"
          git push
